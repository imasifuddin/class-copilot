# Audio capture: soundcard drives WASAPI loopback (speaker output),
# sounddevice drives microphone input.
soundcard>=0.4.3
sounddevice>=0.4.6
numpy>=1.24

# Speech to text (CPU friendly, runs fully offline)
faster-whisper>=1.0.3

# Local UI server
fastapi>=0.110
uvicorn[standard]>=0.29

# Terminal QR code so you can point your phone at the laptop screen
qrcode>=7.4

# LLM providers (install only the one you use)
anthropic>=0.40
openai>=1.30

python-dotenv>=1.0

# Reading the material you upload (resume, notes, slides)
pypdf>=4.0
python-docx>=1.1
